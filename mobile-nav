
  <!-- ==================================================================
       Mobile Bottom Navigation & Services Menu (Small Screens)
       ================================================================== -->
 <nav id="mobileNav" role="navigation" aria-label="Mobile Navigation">
  <div class="nav-section">
    <button id="langToggle" aria-label="Language Toggle">EN</button>
    <button id="themeToggle" aria-label="Theme Toggle">Light</button>
  </div>
  <div class="nav-section">
    <button id="chatOption" aria-label="Chatbot Option">Chatbot</button>
  </div>
  <div class="nav-section">
    <button id="hamburgerButton" aria-label="Menu Toggle">☰</button>
    <div id="hamburgerMenu" class="hidden">
      <a href="business.html">Business Operations</a>
      <a href="contact.html">Contact Center</a>
      <a href="it.html">IT Support</a>
      <a href="professionals.html">Professionals</a>
    </div>
  </div>


/* Mobile Overrides for Screens ≤ 768px */
@media screen and (max-width: 768px) {
  /* Hide desktop navigation elements */
  header nav ul,
  .toggle-container {
    display: none;
  }

  /* Show mobile navigation */
  #mobileNav {
    display: flex;
    position: fixed;
    bottom: 0;
    left: 15%;      /* 15% margin from left */
    width: 85%;     /* occupies the right 85% of the screen */
    background: #fff;
    border-top: 1px solid #ccc;
    justify-content: space-around;
    align-items: center;
    padding: 0.5rem 0;
    z-index: 3000;
    box-shadow: 0 -2px 5px rgba(0,0,0,0.1);
    transition: transform 0.3s ease, background-color 0.3s ease;
  }
